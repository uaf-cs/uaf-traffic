CREATE TABLE pins (
`pin` VARCHAR(4) UNIQUE,
`expires` DATETIME NOT NULL DEFAULT (DATETIME(CURRENT_TIMESTAMP, '+30 minutes'))
);

CREATE TRIGGER old_pins AFTER INSERT ON pins
    BEGIN
        DELETE FROM pins WHERE expires < DATETIME(CURRENT_TIMESTAMP);
    END;
